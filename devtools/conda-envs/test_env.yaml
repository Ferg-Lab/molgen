name: test
channels:

  - conda-forge
  - pytorch
  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # package dependencies
  - pytorch
  - pytorch-lightning
  - einops

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

